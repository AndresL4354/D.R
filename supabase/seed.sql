-- =============================================================================
-- seed.sql  —  Datos mínimos para desarrollo local (`supabase db reset`)
-- =============================================================================
-- No commitear datos reales/PII aquí. Solo catálogos y un usuario de prueba.

-- Ejemplo (descomentar cuando exista el esquema):
-- insert into public.perfil (id, login, empresa, roles, activated)
-- values ('00000000-0000-0000-0000-000000000001', 'dev@local', 'SERVICIOS ALTA SPA',
--         array['ROLE_ADMIN','ADMIN_VERTICAL'], true)
-- on conflict (id) do nothing;
